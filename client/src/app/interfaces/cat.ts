export interface CatFilter {
  spay: boolean;
  kitten: boolean;
  gender: string;
}

export interface Cat {
  name: string;
  color: string;
  gender: string;
  age: string;
  spay: string;
  prof_url: string;
  user_id: number;
}
